
  # Dental Clinic Management UI

  This is a code bundle for Dental Clinic Management UI.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.

  en el backend en caso de que el login de error 500

- npm install jsonwebtoken
- npm install -D @types/jsonwebtoken


  
--solo si falla las consultas:
npm install axios


